#include <stdio.h>
int main(int argc, char const *argv[]) {
        int n=0,y=1;
        int PI=3.14;
        char *str = “Sanfoundry.com\0” “training classes”;
        puts(str);
        y==1?(n=0):(n=1);
        if(n)
                printf("yes\n" );
        else
                printf("%s\n","no" );
        return 0;
}
