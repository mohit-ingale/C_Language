/*Develop a C program which adds all numbers from 1 to N, except those which are divisible by 5. Implement this using for loop and continue statement.*/
#include <stdio.h>
void main()
{
	int i,sum=0,n;
	printf("enter last value: ");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(i%5==0)					//Omit numbers divisible by 5
			continue;
		sum+=i;
	}
	printf("\n sum of values from 1 to %d : %d\n",n,sum);
}
