//Write a program to find the sum of digits of a given number.
#include<stdio.h>
void main()
{
	unsigned int a,sum;
	while(1)
	{
	sum=0;
	printf("\nEnter a number:");
	scanf("%d",&a);

		while(a>0)
		{
			sum=sum+(a%10);
			a/=10;
		}
	printf("Sum of digits=%d\n",sum);
	}
}

